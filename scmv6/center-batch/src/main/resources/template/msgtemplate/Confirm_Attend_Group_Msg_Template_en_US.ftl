<#if (msgTitle?exists)  >  
   Invitation to Join Group:${groupName}Reminder

<#else>
<a href="${psnUrl}">${psnName}</a> invites you to join group:<a href="${groupUrl}">"${groupName}"</a>。
  
</#if>

  
  